/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-organize-imports'],
  arrowParens: 'avoid',
  trailingComma: 'none',
  semi: false,
  singleQuote: true,
  tabWidth: 2
}

export default config
